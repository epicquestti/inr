import { arrayMultipleSchemas, returnArray } from "@lib/types/insertChecked"

const insertCheckedFunction = async (
  array1: arrayMultipleSchemas[],
  array2: arrayMultipleSchemas[]
): Promise<returnArray[]> => {
  return []
}

export default insertCheckedFunction
